-- BEGIN;
-- SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
-- UPDATE contas SET saldo = saldo -1000 WHERE conta_id = 1;
-- COMMIT;

BEGIN;
-- SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
UPDATE contas SET saldo = saldo -1000 WHERE conta_id = 1;
COMMIT;
